#include "main.h"

/**
 * _abs - finction that prints the absolute value of an integer
 * @int: the inpt we make positive always
 * Return: if positive return the real number and if
 * negative return the neg of the value
**/

int _abs(int)
{
	int x;

	if (x >= 0)
	{
		return (x);
	}
	if (x < 0)
	{
		return (-x);
	}
}
